{
  "name": "orch-mock-server",
  "version": "1.0.0",
  "private": true,
  "description": "ORCH local mock API server",
  "scripts": {
    "start": "node server.js",
    "start:delay": "node server.js --delay 200",
    "start:auth": "node server.js --auth",
    "start:full": "node server.js --auth --delay 200 --errors"
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "ws": "^8.18.0"
  }
}
