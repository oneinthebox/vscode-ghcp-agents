# Dependency Verification Report

Generated by `setup-deps.js` on 2026-03-25.

## Summary

| Ecosystem | Package Manager | Config | Lock File | Install | Verify |
|-----------|----------------|--------|-----------|---------|--------|
| Node.js | npm | Found | package-lock.json | Success | No issues |
| Java | maven | Found | N/A | Success | 2 conflicts |
| Python | uv | Found | uv.lock | Success | No issues |

## Node.js (npm)

**Config:** `.npmrc` found with Artifactory registry configured.

**Lock file:** `package-lock.json` present (147 packages).

**Install output:**
```
added 147 packages in 12s
```

**Verification (`npm ls --all`):**
```
my-app@1.0.0
├── express@4.18.2
├── typescript@5.3.3
├── @types/node@20.11.5
└── ... (147 packages, 0 problems)
```

No peer dependency conflicts detected.

## Java (Maven)

**Config:** `~/.m2/settings.xml` found with Artifactory mirror.

**Lock file:** N/A (Maven does not use lock files by default).

**Install output:**
```
[INFO] Scanning for projects...
[INFO] --- maven-dependency-plugin:3.6.0:resolve (default-cli) @ my-service ---
[INFO] The following files have been resolved:
[INFO]    org.springframework.boot:spring-boot-starter-web:jar:3.2.1
[INFO]    ... (84 dependencies resolved)
[INFO] BUILD SUCCESS
```

**Verification (`mvn dependency:tree`):**
```
[WARNING] Version conflict:
  com.fasterxml.jackson.core:jackson-databind:2.16.0 (managed) vs 2.15.3 (transitive)
  org.slf4j:slf4j-api:2.0.11 (managed) vs 2.0.9 (transitive)
```

Two version conflicts found. Managed versions take precedence; no action required unless runtime issues occur.

## Python (uv)

**Config:** `pyproject.toml` found with Artifactory index URL.

**Lock file:** `uv.lock` present.

**Install output:**
```
Resolved 12 packages in 1.2s
Installed 12 packages in 0.8s
 + fastapi==0.109.2
 + uvicorn==0.27.1
 + pydantic==2.5.3
 + httpx==0.26.0
 + ... (12 packages total)
```

**Verification (`uv pip check`):**
```
All packages are compatible.
```

## JSON Output

```json
{
  "ecosystems": [
    {
      "type": "node",
      "packageManager": "npm",
      "configFound": true,
      "lockFileFound": true,
      "installResult": "success",
      "verifyResult": "no issues"
    },
    {
      "type": "java",
      "packageManager": "maven",
      "configFound": true,
      "lockFileFound": false,
      "installResult": "success",
      "verifyResult": "issues found"
    },
    {
      "type": "python",
      "packageManager": "uv",
      "configFound": true,
      "lockFileFound": true,
      "installResult": "success",
      "verifyResult": "no issues"
    }
  ]
}
```
